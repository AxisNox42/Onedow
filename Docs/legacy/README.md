# Legacy project files

이 폴더의 파일은 2026-09-20 구조 정리 전에 사용하던 리소스 위치를 보존한 것이다.
현재 빌드와 실행에 사용하는 기준 경로는 `WiNILL/Resource/`이며, 이 폴더의 파일을
새 코드에서 참조하지 않는다.

- `Resource/`: 이전 루트 리소스 묶음
- `WiNILL-Font/`: 이전 macOS 폰트 묶음
