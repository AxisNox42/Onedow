# WiNILL(Onedow) 구현 경험

> Win32 데스크톱 위에 투명 오버레이로 겹쳐지는 "가짜 OS 창" 안에서만 전투가 벌어지는 로그라이트를, C++17 + OpenGL 3.3으로 밑바닥부터 만들었습니다. 이 문서는 해커톤 제출용으로, 실제 개발 과정에서 무엇을 어떻게 구현했는지 — 그리고 AI 코딩 에이전트(Cursor + Claude)를 어떻게 활용했는지를 숨기지 않고 정리한 기록입니다.

---

## 1. 프로젝트 개요

**WiNILL(배포명 Onedow)**은 실제 바탕화면 위에 투명한 풀스크린 오버레이를 띄우고, 그 위에 "가짜 OS 창"들을 드래그로 움직이며 침입한 프로세스(적)를 막아내는 탄막 로그라이트입니다.

- 플레이어는 `onedow.exe` 창 자체이고, 그 창을 움직여 이동·회피합니다.
- 창 밖은 시야 밖 — 보이는 창 영역 안에서만 전투·피격·이펙트가 발생합니다.
- 웨이브를 버티며 EXP를 모아 레벨업 → 증강(버프) 3택1 + 디버프 강제 1개를 고르고, 점수가 쌓이면 별도의 "보스 창"이 열리며 보스전이 시작됩니다.
- 런이 끝나면 코인으로 영구 스탯을 올리는 메타 상점, 도감, 업적, 직업 해금 등 로그라이트 표준 메타 루프가 붙어 있습니다.

기술 스택은 C++17, OpenGL 3.3(glad) + GLFW + glm, Win32 DWM(투명 합성), 오디오는 miniaudio, 텍스트는 FreeType 기반 자체 `TextRenderer`입니다. Windows 빌드가 메인이지만 macOS 크로스 빌드도 별도로 대응해뒀습니다.

---

## 2. 핵심 기술 구현

### 2-1. "가짜 OS 창"을 진짜처럼 보이게 하기 — DWM 투명 오버레이

`glfwWindowHint(GLFW_TRANSPARENT_FRAMEBUFFER, GLFW_TRUE)`로 알파 채널이 살아있는 풀스크린 GLFW 창을 만들고, Windows에서는 `DwmExtendFrameIntoClientArea`로 per-pixel 알파 합성을 켜서 바탕화면과 다른 앱이 뒤로 그대로 비치게 만들었습니다. 그 투명 캔버스 위에다 타이틀바·테두리를 가진 "가짜 창"(`FakeWindow`)들을 직접 그려서, 실제로는 하나의 오버레이인데 사용자에게는 여러 개의 독립된 OS 창처럼 보이게 만드는 게 이 게임의 핵심 트릭입니다.

### 2-2. Immediate Draw 없이 CPU 배치 렌더링

렌더링은 전부 CPU 쪽 `std::vector<float> g_Batch`에 `[x, y, r, g, b, a]` 정점을 쌓아두고 `BatchFlush()`가 호출될 때 한 번에 GL로 업로드해 그리는 구조입니다. 도형 하나 그릴 때마다 draw call을 날리지 않기 위한 선택이었고, 대신 규칙이 하나 생겼습니다: **Scissor/Blend/Shader 상태를 바꾸기 직전에는 반드시 Flush를 먼저 해야 한다.** 이 규칙을 어기면 이전 프레임 도형이 새 클리핑 영역에 잘못 그려지는 버그가 나서, 프로젝트 전체 규칙(`CLAUDE.md`)에 "CRITICAL"로 못박아두고 매번 지켰습니다.

### 2-3. 창마다 독립된 시야 — WorldScissor 멀티 윈도우 클리핑

가짜 창은 여러 개가 동시에 존재합니다(플레이어 창, 보스 창, 포탑 창, 이벤트성 팝업 창 등). 각 창은 자기 영역 밖의 월드를 보여주면 안 되기 때문에, 월드 좌표를 줌 배율까지 반영해 GL `glScissor` 사각형으로 변환하는 `WorldScissor(wx, wy, ww, wh)`를 만들고, 여러 창에 걸쳐 보이는 오브젝트(보스의 잔해 이펙트, 팝업, 원거리 투사체 등)는 **창마다 렌더 패스를 통째로 반복**하도록 아키텍처를 잡았습니다. 처음에는 "보스 창 안에서만 보이면 되지 않나" 하고 단순하게 갔다가, 보스 능력이 화면 곳곳으로 퍼지는 디자인이 늘어나면서 이 멀티 패스 구조 없이는 감당이 안 된다는 걸 깨닫고 리팩터링했습니다.

---

## 3. 보스 디자인 — 반복(iteration)이 곧 구현 경험이었다

이 프로젝트에서 가장 시간을 많이 쓴 부분은 사실 "한 번에 잘 만드는 것"이 아니라 **만들고 실제로 플레이해보고, 안 맞으면 갈아엎는 사이클**이었습니다. 최종적으로 살아남은 활성 보스는 3개(`VOLLEY.sys`, `FORK.worm`, `SPAM.dll`)지만, 그 뒤에는 몇 번의 완전 재설계가 있었습니다.

**사례 1 — 인터셉터 함대 → 완전 폐기.**
전투기 무리 AI(플레이어를 향해 사방으로 튀는 인터셉터, 임팩트-존 야마토포, 회전 십자 강타)를 설계·구현하고 나서 실제로 플레이해보니 "전투기가 그냥 몸통 박치기로 싸운다"는 게 말이 안 된다는 피드백이 나왔습니다. 이동 방식을 궤도 기동 + 유인/저격 페어 구조로 다시 짜고 나서도, 결국 컨셉 자체가 기대와 다르다는 판단이 들어 통째로 새 보스(`C2_RELAY.sys` 리메이크)로 갈아탔습니다.

**사례 2 — 촉수 보스가 두 번 죽었다.**
`C2_RELAY.sys`는 "명령 흐름 차단 + 엘리트 호스트 3기"로 한 번 설계했다가, "Rain World의 장님 거미" 레퍼런스가 나오면서 다시 "소음 기반 탐지 + 그랩·드래그 촉수" 컨셉으로 재설계했습니다. 결과물의 외형 컨셉은 좋았지만, 정작 넣을 수 있는 능력 대부분이 예상 가능한 범주라는 지적을 받고 결국 이 컨셉도 활성 로스터에서 제외했습니다. 코드는 지우지 않고 "비활성" 상태로 남겨서 나중에 다시 살릴 수 있게 했습니다.

**사례 3 — `LAG.exe`: 만들고, 체감하고, 전부 갈아엎었다.**
"프레임 지연"을 테마로 한 보스를 만들면서 처음엔 "게임 전체를 주기적으로 버벅이게 만드는" 전역 스터터 기믹을 구현했습니다. 빌드해서 직접 플레이해보니 세 가지 문제가 드러났습니다: 이펙트가 부족했고, 기존 보스(`GLITCH.exe`)의 글리치 비주얼을 그대로 재활용해서 시각적으로 겹쳤고, 무엇보다 **"내 조작을 강제로 뺏는" 방식 자체가 재미없었습니다.** 여기서 얻은 설계 원칙은: *플레이어의 조작감을 직접 빼앗는 디버프성 기믹은, 이론적으로 테마에 맞아도 실제로 당해보면 재미가 아니라 짜증이 된다*는 것이었습니다. 이 교훈으로 핵심 훅을 통째로 "가짜 분신을 흩뿌려 진짜를 판별하게 하는" 방식으로 바꿨지만, 결국 이 보스는 최종적으로 폐기하고 처음부터 다른 컨셉(`SPAM.dll`)으로 새로 만들었습니다.

**사례 4 — `SPAM.dll`: 교훈을 반영해서 다시 설계.**
"화면을 뒤덮는 팝업창" 컨셉으로, 이번엔 처음부터 **플레이어의 조작을 건드리지 않는** 방향으로 설계했습니다. 오류 다이얼로그 모양의 팝업들이 보스 코어 주변에 쌓여 코어를 무적으로 만들고(창=시야를 가리는 방패), 방치하면 시한폭탄처럼 터지고, 부수면 안전하게 사라집니다. 2페이즈에서는 "업데이트 설치 중..." 게이지가 차오르는 동안 코어가 완전 무적이 되는데, 이 시간 동안 팝업을 얼마나 치웠는지가 게이지가 끝난 뒤 터지는 공격의 규모를 결정합니다 — 조작을 뺏지 않으면서도 순간적인 판단을 요구하는 구조로, `LAG.exe`에서 얻은 교훈을 직접 반영한 결과물입니다.

이 반복 과정에서 실제로 만든 뒤 지운 코드가 살린 코드보다 많습니다. 하지만 그 폐기 과정 하나하나가 "이 게임에서 어떤 기믹이 재미있고 어떤 게 아닌지"에 대한 구체적인 기준을 만들어준, 가장 값진 구현 경험이었습니다.

---

## 4. 로그라이트 메타 시스템

보스 전투 말고도 표준적인 로그라이트 루프를 갖추는 데 상당한 시간을 썼습니다.

- **증강(Augment) 시스템**: 등급(COMMON~MYTHIC)별 80종 이상, 디버프 강제 픽(리스크=리턴), 조합 증강·신화 티어까지 있는 빌드 시스템.
- **이중 경제**: 런 중에만 쓰는 "런 골드"(보스 인터미션 상점)와, 런이 끝나야 정산되는 영구 코인(메타 상점) 두 트랙으로 분리해 단기 빌드업과 장기 성장을 모두 챙기게 했습니다.
- **도감/업적/직업**: 처음 보는 증강·적·보스는 `???`로 가려두고 실제로 마주쳐야 해금되는 도감, 업적 달성 시 직업(암살자/광전사/폭격수 등) 해금 구조.
- **세이브 시스템**: 설정·최고 점수·코인·메타 진행도를 XOR 난독화된 로컬 파일로 저장.

---

## 5. AI 활용 경험 (숨기지 않고 적습니다)

이 프로젝트는 **1인 개발 + Cursor의 AI 코딩 에이전트(Claude 기반)를 실질적인 구현 파트너로 쓴 개발**입니다. 커밋 로그에도 그대로 남아있습니다 — 최근 작업 다수가 에이전트 세션이 끝날 때 자동으로 찍히는 `backup: (auto)` 커밋이고, `.cursor/rules/`, `CLAUDE.md`에 "새 파일 만들면 반드시 vcxproj에 등록", "렌더링은 반드시 배치 후 Flush" 같은 프로젝트 규칙을 문서화해서 AI 에이전트가 매 세션 다시 읽고 지키게 만들었습니다.

실제 협업 방식은 이랬습니다.

1. **제가 방향을 던지면, 에이전트가 옵션을 만들어 왔습니다.** "이번 보스 다시 기획해보자" 같은 모호한 요청에 에이전트가 정체성/외형/이동/공격 패턴별로 A/B/C 선택지를 만들어 제시하고, 제가 고르면 그 조합으로 실제 C++ 클래스를 구현하는 흐름을 반복했습니다. 위 3절의 보스 4개 모두 이 방식으로 나왔습니다.
2. **저는 빌드해서 직접 플레이하고, 감(感)으로 피드백했습니다.** "인터셉터가 너무 빠르고 몸통 박치기 하는 것 같다", "버벅거리는 걸 당해보니까 별로다" 같은, 코드가 아니라 플레이 경험 언어로 피드백을 주면 에이전트가 그걸 구체적인 수치/구조 변경으로 번역해서 반영했습니다.
3. **대형 리팩터/삭제도 에이전트가 전담했습니다.** 보스를 하나 폐기할 때마다 `main.cpp`, `BossDirector.h`, `Codex.h`, `Scenes.cpp`, `TutorialText.h`, `SaveSystem.h`, `.vcxproj`까지 8~9개 파일에 흩어진 참조를 전부 찾아 정리하고, 새 보스를 그 자리에 앉히는 작업(파일 생성 → 각 시스템 연동 → 빌드 확인)을 매번 통째로 처리했습니다. 사람이 직접 하면 실수하기 쉬운 "이 이름이 남긴 참조가 몇 개 파일에 더 있더라" 같은 부분에서 AI의 grep 기반 전체 탐색이 특히 유용했습니다.
4. **빌드 검증은 매번 실제로 돌렸습니다.** 기능을 새로 넣거나 지울 때마다 `msbuild`로 실제 빌드해서 에러가 없는 상태로만 다음 단계로 넘어가는 걸 원칙으로 삼았습니다 — AI가 만든 코드를 "그럴듯해 보이니 믿는다"가 아니라 매번 컴파일 통과를 확인하고 넘어갔습니다.

AI를 썼다고 해서 설계를 아웃소싱한 건 아니었습니다. "이 기믹이 재미있는가"를 판단하는 건 항상 실제로 플레이해본 사람의 몫이었고, 3절의 `LAG.exe` 폐기 사례처럼 — 이론적으로는 말이 되는 기획도 실제로 당해보면 별로일 수 있다는 판단은 AI가 대신해줄 수 없는 부분이었습니다. AI는 "그 판단을 코드로 빠르게 구현하고, 실패하면 빠르게 갈아엎는" 반복 속도를 극단적으로 끌어올려준 도구였습니다.

---

## 6. 회고

가장 크게 배운 것은 "완벽한 설계를 미리 다 하고 시작하기"보다 "빨리 만들어서 직접 당해보고 버리는" 사이클이 이 장르(탄막 로그라이트)에서는 훨씬 정확하다는 점입니다. 문서 위에서는 재밌어 보이는 기믹(전역 스터터, 몸통 박치기 인터셉터)이 실제로 플레이해보면 전혀 다르게 느껴지는 경우가 반복적으로 있었고, 그 격차를 줄이는 유일한 방법은 실제로 빌드해서 플레이하는 것뿐이었습니다. AI 코딩 에이전트는 이 "구현 → 플레이 → 피드백 → 재구현" 루프의 반복 속도를 크게 올려줬고, 그 덕분에 한 세션 안에서 보스 하나를 통째로 두세 번 갈아엎는 게 가능했습니다.
